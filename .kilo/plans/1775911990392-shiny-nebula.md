# server/app/routes/media.py 版本历史

## 版本 1: `bce79e0` — 初始创建
**日期**: 2026-04-05 13:27  
**提交信息**: feat: add WeChat Channels, WeChat MP, and Xiaohongshu plugins  
**变更**: 新建文件 (146 行)

### 差异
```diff
+ 新增完整的媒体管理路由文件，包含：
+ - POST /api/media/upload — 通用文件上传
+ - POST /api/media/upload-to-article/{article_id} — 上传到指定文章目录
+ - GET /api/media — 获取媒体列表（支持 media_type、article_id 过滤）
+ - DELETE /api/media/{media_id} — 删除媒体文件
```

### 总结
初始版本，实现了媒体文件的 CRUD 基础功能：上传、关联文章上传、列表查询、删除。使用 SnowID 生成唯一文件名，文件存储在 UPLOADS_DIR。

---

## 版本 2: `9242547` — 代码重构
**日期**: 2026-04-06 15:46  
**提交信息**: Refactor code structure for improved readability and maintainability  

### 差异
```diff
+ import logging, Query
+ from ..config import MAX_UPLOAD_SIZE, ALLOWED_EXTENSIONS
+ 新增 validate_file_extension() 函数
  upload_file:
+   文件扩展名白名单校验
+   文件大小限制 (MAX_UPLOAD_SIZE)
+   OSError 异常处理
+   新增 audio 媒体类型支持
  upload_to_article:
+   同样的扩展名校验和大小限制
+   OSError 异常处理
+   file_path 存储改为 relative_path
  get_media_files:
+   使用 Query() 添加分页参数验证 (skip>=0, 1<=limit<=500)
  delete_media_file:
+   HTTP 204 No Content 响应
+   支持相对路径解析
+   OSError 安全删除处理
```

### 总结
安全加固版本：增加了文件扩展名白名单校验、文件大小限制、`audio` 媒体类型支持、分页参数验证，并添加了全面的异常处理和日志记录。

---

## 版本 3: `2b55288` — 用户专属存储路径
**日期**: 2026-04-06 22:37  
**提交信息**: feat: implement user-specific storage paths for articles and videos, add settings management  

### 差异
```diff
+ from datetime import datetime, timezone
+ from ..config import BASE_STORAGE_DIR
+ from ..routes.settings import (get_articles_dir, get_videos_dir, _get_base_storage_dir)
+ from .articles import _article_dir as _calc_article_dir
+ 新增 _get_content_dir() — 根据 article_type 获取不同存储目录
+ 新增 _resolve_article_dir() — 智能解析文章目录路径
  upload_to_article:
+   文件保存到 images/ 子目录
+   文件名增加时间戳前缀 (YYYYMMDD_HHMMSS_snowid.ext)
+   路径改为相对 BASE_STORAGE_DIR 的相对路径
  delete_media_file:
+   区分关联文章和独立媒体文件的删除逻辑
+   关联文章时从 BASE_STORAGE_DIR 解析路径
```

### 总结
引入用户专属存储路径体系，根据 article_type (video/article) 使用不同的存储目录，上传文件放入 `images/` 子目录并添加时间戳前缀，删除逻辑也随之更新。

---

## 版本 4: `b69111d` — 移动端支持与复制功能
**日期**: 2026-04-07 12:08  
**提交信息**: feat: update article preview and editor functionality to support mobile and desktop platforms, enhance image upload process  

### 差异
```diff
+ import shutil
  upload_file:
+   增加详细日志记录（文件上传和数据库记录创建）
  upload_to_article:
+   增加详细日志记录
+ 新增 POST /copy-to-article/{article_id}/{media_id} 端点:
+   将已有媒体文件复制到目标文章目录
+   使用 shutil.copy2 保留文件元数据
+   更新 media.file_path 和 article_id
```

### 总结
增加了详细的日志记录用于调试，并新增了 `copy-to-article` 端点，支持将已有媒体文件复制到其他文章（跨文章引用图片场景）。

---

## 版本 5: `a67d3d0` — 存储路径重构
**日期**: 2026-04-07 16:32  
**提交信息**: feat: restructure media library storage paths and update related file handling  

### 差异
```diff
- import os
- from ..config import UPLOADS_DIR
+ from ..config import BASE_STORAGE_DIR
+ from ..routes.settings import get_media_dir as _get_media_dir
  upload_file:
-   file_path = UPLOADS_DIR / safe_filename
+   media_dir = _get_media_dir(db, current_user.id)
+   file_path = media_dir / safe_filename
-   file_path=f"uploads/{safe_filename}"
+   relative_path = str(file_path.relative_to(BASE_STORAGE_DIR))
+   file_path=relative_path
  delete_media_file:
-   区分 article_id 和非关联的复杂逻辑
+   统一使用 BASE_STORAGE_DIR / file_path 解析
```

### 总结
移除了对固定 UPLOADS_DIR 的依赖，改为使用用户专属的 media_dir（通过 `_get_media_dir` 获取），文件路径统一为相对 BASE_STORAGE_DIR 的相对路径，删除逻辑大幅简化。

---

## 版本 6: `f46b301` — Milkdown 编辑器集成
**日期**: 2026-04-07 18:02  
**提交信息**: feat: enable image upload in Milkdown WYSIWYG mode and display associated article titles in media library  

### 差异
```diff
+ from sqlalchemy.orm import joinedload
  get_media_files:
+   .options(joinedload(Media.article)) — 预加载关联文章
+   .unique().all() — 配合 joinedload 去重
  delete_media_file:
+   仅当 article_id is None 时才删除物理文件（关联文章的图片不再删除文件）
```

### 总结
为媒体列表查询添加 `joinedload` 预加载关联文章（用于显示文章标题），并改进删除逻辑——关联文章的图片文件不再随媒体记录删除而删除（因为文章仍需引用）。

---

## 版本 7: `2743390` — Markdown 路径支持
**日期**: 2026-04-08 10:24  
**提交信息**: feat: enhance media handling with article-relative paths and markdown support  

### 差异
```diff
  upload_to_article:
-   str(article_dir.relative_to(...))
+   .as_posix() — 使用 POSIX 风格路径（正斜杠）
+   markdown_path = f"images/{safe_filename}"
+   resp.markdown_path = markdown_path — 返回 Markdown 可直接引用的相对路径
  copy-to-article:
+   同样返回 markdown_path 字段
```

### 总结
关键改进：上传/复制文件后返回 `markdown_path` 字段，供编辑器直接插入 Markdown 图片引用。所有路径统一使用 POSIX 格式（正斜杠）确保跨平台兼容。

---

## 版本 8: `ddea1a1` — 循环依赖修复
**日期**: 2026-04-08 15:31  
**提交信息**: refactor(editor): eliminate circular dependency in auto-save and improve logic  

### 差异
```diff
  upload_to_article / copy-to-article:
    代码格式化：将长行拆分为多行
  get_media_files:
-   .unique().all()
+   .all() — 移除 unique() 调用
```

### 总结
小幅修复：移除 get_media_files 中不必要的 `.unique()` 调用，并对长行进行格式化（代码风格调整）。

---

## 版本 9: `6291bbf` — 删除逻辑修复（当前版本）
**日期**: 2026-04-08 17:43  
**提交信息**: fix: remove media  

### 差异
```diff
  delete_media_file:
-   统一 BASE_STORAGE_DIR / file_path 删除
+   if media.article_id is not None:
+       通过 media_dir / snow_id+ext 定位原始媒体文件并删除
+   else:
+       独立媒体文件仍使用 BASE_STORAGE_DIR / file_path 删除
```

### 总结
修复删除逻辑：区分关联文章的媒体和独立上传的媒体。关联文章的媒体通过 `_get_media_dir` + snow_id 定位原始文件进行删除；独立上传的媒体使用相对路径解析删除。确保删除操作能正确找到并移除物理文件。
